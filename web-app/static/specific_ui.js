import UI from "./ui_utilities.js";
const Page = Object.create(null);

// Code is written in an imperative style

// extentension of main.js if you wanted to split that out.

// You could just use main.js

// Code runs when it is loaded.

UI.el("some_element").onclick = function () {

};

export default Object.freeze(Page);
