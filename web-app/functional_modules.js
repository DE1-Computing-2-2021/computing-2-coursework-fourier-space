import general_utility from "./general_utilities.js";
const Module = Object.create(null);

// Code is written in a functional style
// Doesn't *do* anything, but provides functions for other to use.

// Specific in context to the problem being solved.

export default Object.freeze(Module);
